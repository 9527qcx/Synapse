# ============ Synapse 阶段 1 依赖 ============

# ---- 核心库（阶段 1 必需）----
pydantic>=2.12          # 数据 Schema 校验（§17 全靠它）
pydantic-settings>=2.10 # 从 .env 读配置
python-dotenv>=1.1      # .env 文件加载
chromadb>=1.5           # 向量记忆库（嵌入式）
openai>=1.40            # DeepSeek API 调用（OpenAI 兼容协议）
requests>=2.32          # 网页请求
httpx>=0.27             # Ollama 本地模型调用
tiktoken>=0.8           # token 估算（可选路径）
arxiv>=2.1.0            # 论文检索
trafilatura>=1.12       # 网页正文提取
langfuse>=3.0           # 全链路追踪
sentence-transformers>=3.0  # 本地嵌入模型 BGE-M3
torch>=2.5              # 嵌入模型依赖（CPU 版即可）

# ---- 开发与测试 ----
pytest>=8.0
pytest-cov>=5.0         # 覆盖率统计（90% 门禁）
black>=24               # 代码格式化
isort>=5.13             # import 排序

# ---- 后续阶段预留（本期不安装）----
# langgraph   # 阶段 2 编排
# streamlit   # 阶段 4 可视化
# neo4j       # V2 图记忆
# pymupdf     # 深度阅读 PDF 解析
